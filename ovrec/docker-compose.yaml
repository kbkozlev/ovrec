version: '3.8'

services:
  web:
    image: nginx:alpine
    container_name: ovrec
    volumes:
      - ./public:/usr/share/nginx/html
      - ./nginx/default.conf:/etc/nginx/conf.d/default.conf
    ports:
      - "80:80"
    restart: unless-stopped
